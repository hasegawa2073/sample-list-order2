{
  "name": "sample-list-order2",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "watch": "npm run watch:sass & npm run watch:ts",
    "watch:sass": "sass src/scss/:dist/css/ --no-source-map --watch",
    "watch:ts": "tsc -watch"
  },
  "devDependencies": {
    "sass": "^1.54.9",
    "typescript": "^4.8.2"
  }
}
